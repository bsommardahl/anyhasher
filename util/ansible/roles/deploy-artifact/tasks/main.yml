---
- name: Deploy Backend Artifact
  synchronize: # "copy" takes too long!
    src: "../../backend/build"
    dest: ~/anyhasher

- name: Start Server
  shell: "cd ~/anyhasher/build && pm2 restart server.config.js"

- name: Verify Server
  uri:
    url: http://localhost:5000/hash/123
